<div class="form-newsletter">
    <span class="item-title">Nechceme na tebe kašlat.</span>
    <span class="text-md">Když se objeví něco zásadního, včas ti dámě vědět e-mailem:</span>

    <form n:name="form" class="mt-md">
        <div class="form-row errors" n:if="$form->hasErrors()">
            <div n:foreach="$form->errors as $error">{$error}</div>
        </div>
        <div class="form-row">
            <input n:name="email" placeholder="Tvoje emailová adresa">
        </div>
        <div class="form-row">
            <div class="checkbox">
                <input n:name="consent">
                <label n:name="consent">
                    Souhlasím se zpracováním osobních údajů de zákona č. 101/2000 Sb.
                </label>
            </div>
        </div>
        <div class="form-row">
            <button n:name="submit" class="btn btn-action btn-block btn-newsletter-colors">Odeslat</button>
        </div>
    </form>
</div>
